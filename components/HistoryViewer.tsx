// components/HistoryViewer.tsx
import React from 'react';

const HistoryViewer: React.FC = () => {
    // This is a legacy component and is no longer used in the main application flow.
    // It is kept as a null component to prevent import errors if it's referenced somewhere unexpectedly.
    return null;
};

export default HistoryViewer;
