// components/WeeklyInsights.tsx
import React from 'react';

const WeeklyInsights: React.FC = () => {
    // This is a legacy component that has been replaced by more advanced analytics
    // in the Home view (like PerformanceScore and WeeklyProgressAnalysis).
    // It is kept as a null component to prevent any potential import errors.
    return null;
};

export default WeeklyInsights;
