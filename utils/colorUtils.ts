// Versión simplificada que devuelve gradientes básicos
export const useImageGradient = (imageUrl?: string) => {
    return { gradient: 'linear-gradient(135deg, #1e293b, #0f172a)', isLoading: false };
};
export const pSBC = (p: number, c0: string, c1?: string, l?: boolean) => null; // Placeholder